#runtime: python
#env: flex
#entrypoint: gunicorn -b :$PORT main:app

#runtime_config:
#  python_version: 3

runtime: python39
entrypoint: gunicorn -b :$PORT demo:app

handlers:
  - url: /.*
    script: demo.app